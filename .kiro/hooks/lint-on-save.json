{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Run ESLint when TypeScript/React files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["**/*.ts", "**/*.tsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npx next lint"
  }
}
